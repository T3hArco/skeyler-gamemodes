ENT.Base = "base_point"
ENT.Type = "point"

---------------------------------------------------------
--
---------------------------------------------------------

function ENT:Initialize()
end

---------------------------------------------------------
--
---------------------------------------------------------

function ENT:KeyValue(key, value)
--	if (key == "boundingbox") then
--		self.min = Vector(value *-1, value *-1, 0)
--		self.max = Vector(value, value, 0)
--	end
end